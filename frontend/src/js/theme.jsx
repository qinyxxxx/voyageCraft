import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: '#1973d2', // primary theme
    },
    secondary: {
      main: '#9c27b0', // secondary theme
    },
  },
});

export default theme;